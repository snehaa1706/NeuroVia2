# Central Configuration Constants for the application layer.
# Phase 10: All static test data has been removed.
# Orientation answers and recall words are now generated dynamically per session
# and stored in assessment metadata. No hardcoded test content should exist here.

# NOTE: AD8 questions remain static — they are a clinically validated instrument.
# Randomizing AD8 would invalidate the screening tool.
