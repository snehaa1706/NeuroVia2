import ScreeningApp from '../components/ScreeningApp';

export default function ScreeningPage() {
  return (
    <div style={{ minHeight: '100vh', width: '100%', backgroundColor: 'var(--color-bg-page)' }}>
      <ScreeningApp />
    </div>
  );
}
